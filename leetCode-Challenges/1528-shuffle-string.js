/**
 * @param {string} s
 * @param {number[]} indices
 * @return {string}
 */
var restoreString = function (s, indices) {
	if (s.length !== indices.length) return;
	let obj = {};

	for (let el of indices) {
	}
};

restoreString('cba', [2, 1, 0]);
